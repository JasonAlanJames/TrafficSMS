# Optional override for the Next.js development proxy target.
# Defaults automatically to http://localhost:8000 on host development
# and http://api:8000 when the frontend runs inside Docker.
# INTERNAL_API_URL=http://localhost:8000
